import type { DailySummary } from '@/lib/api/types';import { formatDateTime } from '@/lib/formatting/date';
export function DailySummaryCard({summary}:{summary:DailySummary}){return <section className="card section"><h2>Daily summary</h2>{summary.status==='available'?<><p>{summary.short_summary}</p><p><span className="chip">{summary.counters.open_loops} open loops</span> <span className="chip">{summary.counters.decisions} decisions</span> <span className="chip">{summary.counters.people_mentioned} people</span></p><p className="muted">Generated {formatDateTime(summary.generated_at)}</p></>:<p className="muted">No daily summary is available yet.</p>}</section>}
