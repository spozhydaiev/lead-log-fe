import { render,screen,waitFor } from '@testing-library/react';import userEvent from '@testing-library/user-event';import { describe,it,expect,vi,beforeEach } from 'vitest';import { SessionProvider,useSession } from './session-provider';
vi.mock('next/navigation',()=>({useRouter:()=>({replace:vi.fn()})}));
function Probe(){const s=useSession();return <div><p>{s.status}</p><button onClick={s.refreshSession}>refresh</button><button onClick={s.logout}>logout</button></div>}
beforeEach(()=>{process.env.NEXT_PUBLIC_API_BASE_URL='https://api.test';localStorage.clear();sessionStorage.clear();vi.restoreAllMocks()});
it('loads authenticated and refreshes without storage',async()=>{global.fetch=vi.fn().mockResolvedValue(new Response(JSON.stringify({data:{user:{email:'a@b.com',timezone:'UTC',response_language:'en'},telegram:{connected:false,linked_at:null}}}),{status:200})) as typeof fetch;render(<SessionProvider><Probe/></SessionProvider>);expect(screen.getByText('loading')).toBeInTheDocument();await screen.findByText('authenticated');await userEvent.click(screen.getByText('refresh'));await waitFor(()=>expect(fetch).toHaveBeenCalledTimes(2));expect(localStorage.length).toBe(0);expect(sessionStorage.length).toBe(0)});
it('treats 401 as unauthenticated and network as error',async()=>{global.fetch=vi.fn().mockResolvedValue(new Response(JSON.stringify({error:{code:'unauthorized',message:'no',request_id:'r'}}),{status:401})) as typeof fetch;render(<SessionProvider><Probe/></SessionProvider>);await screen.findByText('unauthenticated')});
it('logout calls backend and clears state',async()=>{global.fetch=vi.fn().mockResolvedValueOnce(new Response(JSON.stringify({data:{user:{email:'a@b.com',timezone:'UTC',response_language:'en'},telegram:{connected:false,linked_at:null}}}),{status:200})).mockResolvedValueOnce(new Response(null,{status:204})) as typeof fetch;render(<SessionProvider><Probe/></SessionProvider>);await screen.findByText('authenticated');await userEvent.click(screen.getByText('logout'));await screen.findByText('unauthenticated')});
