'use client';import { useCallback,useEffect,useState } from 'react';import { ApiRequestError } from '@/lib/api/client';import { api } from '@/lib/api/endpoints';import type { SafeApiError,Today } from '@/lib/api/types';
export function useToday(){const[data,setData]=useState<Today|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<SafeApiError|null>(null);const load=useCallback(async()=>{setLoading(true);setError(null);try{setData(await api.today())}catch(e){setError(e instanceof ApiRequestError?e.safe:{kind:'unexpected_error',message:'Something went wrong. Try again.'})}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);return{data,setData,loading,error,reload:load}}
