'use client';import { useSession } from '@/features/auth/session-provider';import { TelegramSettings } from '@/features/telegram/telegram-settings';
export default function Settings(){const{session,logout,status}=useSession();return <div className="stack"><header><p className="chip">Settings</p><h1>Account and connections</h1></header><section className="card section"><h2>Account</h2><p><strong>Name</strong><br/>{session?.user.display_name||'Not set'}</p><p><strong>Email</strong><br/>{session?.user.email}</p><p><strong>Timezone</strong><br/>{session?.user.timezone}</p><p><strong>Response language</strong><br/>{session?.user.response_language}</p></section><TelegramSettings/><section className="card section"><h2>Session</h2><button className="btn danger" onClick={logout} aria-busy={status==='loading'}>Logout</button></section></div>}
