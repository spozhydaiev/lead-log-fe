'use client';import Link from 'next/link';import { useRouter } from 'next/navigation';import { FormEvent,useState } from 'react';import { api } from '@/lib/api/endpoints';import { ApiRequestError } from '@/lib/api/client';import { useSession } from '@/features/auth/session-provider';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');const r=useRouter();const{setAuthenticated}=useSession();async function submit(e:FormEvent){e.preventDefault();if(busy)return;setError('');if(!email||!password){setError('Email and password are required.');return}setBusy(true);try{const s=await api.login({email,password});setPassword('');setAuthenticated(s);r.replace('/today')}catch(e){setError(e instanceof ApiRequestError&&e.safe.kind==='network_error'?e.safe.message:'Unable to sign in with these credentials.')}finally{setBusy(false)}}return <form className="stack" onSubmit={submit}><h2>Sign in</h2><label className="field">Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="field">Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="btn primary" disabled={busy} aria-busy={busy}>{busy?'Signing in…':'Sign in'}</button><Link href="/register">Create account</Link></form>}
